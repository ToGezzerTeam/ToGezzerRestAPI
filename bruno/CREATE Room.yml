info:
  name: CREATE Room
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/rooms
  body:
    type: json
    data: |-
      {
        "name": "Test room",
        "channelType": "TEXT"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
